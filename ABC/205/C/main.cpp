#include <algorithm>
#include <cmath>
#include <iomanip>
#include <iostream>
#include <map>
#include <queue>
#include <set>
#include <string>
#include <tuple>
#include <vector>

using namespace std;
using ll = long long;
#define rep(i, n) for (int i = 0; i < (int)n; ++i)
constexpr int INF = 1 << 30;

int main()
{
    int A, B, C;
    cin >> A >> B >> C;

    // if ((ll)pow(A, C) > (ll)pow(B, C)) cout << ">" << endl;
    // else if ((ll)pow(A, C) < (ll)pow(B, C)) cout << "<" << endl;
    // else cout << "=" << endl;

    return 0;
}